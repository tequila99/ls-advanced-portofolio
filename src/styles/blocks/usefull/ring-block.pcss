.ring-block {
  display: flex;
  flex-direction: column;
}
.ring-row {
  display: flex;
  flex-direction: column;
  margin-top: 50px;
  &:first-child {
    margin-top: 0;
  }
  &__title {
    font-size: 24px;
    font-weight: 700;
    @include tablets {
      text-align: center;
    }
  }
  &__list {
    margin-top: 50px;
    display: flex;
    @include tablets {
      margin-top: 20px;
    }
    @include phones {
      justify-content: center;
      flex-wrap: wrap;
      @include phones {
        margin-right: -10px;
      }      
    }
    li {
      margin-right: 60px;
      @include tablets {
        margin-right: 15px;
      }
      @include phones {
        margin-right: 10px;
      }
  
      &:last-child {
        margin-right: none;
      }
    }
  }
  &__ring {
    /* margin-right: 60px;
    @include tablets {
      margin-right: 15px;
    }
    @include phones {
      margin-right: 10px;
    }
    &:last-child {
      margin-right: none;
    } */
  }
}